#include <stdio.h>

int main(void){
float x, y, z, caixa_total, custo_total, material, obra;
    printf("Informe a altura: ");
    scanf("%f", x);
    printf("Informe a largura: ");
    scanf("%f", y);
    printf("Informe o comprimento: ");
    scanf("%f", z);
    caixa_total = ()+()+();

    return 0;
}